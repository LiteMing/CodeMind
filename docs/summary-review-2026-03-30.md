# 2026-03-30 审查总结

## 完成内容

- 审查了 `docs/review-2026-03-28.md` 之后的 6 个提交以及当前 `main` 分支相关实现。
- 补做了两类验证：
  - 代码级验证：`go test ./...`
  - 构建级验证：`npm run build:web`
- 对已有桌面产物做了只读检查，确认 Windows manifest 已被错误回写。
- 输出了本轮正式审查文档：`docs/review-2026-03-30.md`

## 发现汇总

- 高优先级问题 1 个：
  - 打包脚本把未渲染的 manifest 模板重新写进了 EXE，现有 `CodeMind-1.0.0.exe` 已可直接检出错误占位符。
- 中优先级问题 2 个：
  - 版本号存在双份来源，下一次升级版本会触发桌面打包失败。
  - 节点高度估算与新增位置计算脱节，带 badge 的节点容易造成新增节点重叠。

## 遗留问题

- 当前没有前端自动化测试覆盖节点尺寸估算、快照交互和桌面打包元数据校验。
- 工作区仍然存在用户未提交改动与构建产物，本轮未做清理或回退。

## 后续建议

1. 让 manifest 和 version info 都从 `wails.json` 或同一个生成步骤产出，不再直接回读模板文件。
2. 在打包脚本里增加对 manifest 内容的自动检查，而不只检查图标和 `ProductVersion`。
3. 修正 `nextStackedY` 的高度来源，并补一个覆盖 `priority` / `childCount` badge 的布局回归验证。
