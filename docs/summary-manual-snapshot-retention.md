# 手动快照保留策略修复总结

日期：2026-07-10 ｜ 分支：feat/ai-rollback-visibility

## 完成内容

- 每图快照总量继续限制为 14 条，未扩大 localStorage 使用上限。
- 裁剪时固定保护最近 4 条手动快照，其余名额由 manual/auto/AI 快照按时间共享。
- 保存和读取路径统一使用同一裁剪函数，旧数据与新写入遵循相同规则。
- AI 前置快照调用处增加 fail-closed 注释：快照持久化失败必须中止后续 AI 写入。
- 新增 3 个独立用例，覆盖高频 AI 写入、无手动快照以及额外手动快照占用共享名额。

## 验证结果

- `cd frontend && npm test`：13 个测试文件，216 通过、2 跳过。
- `cd frontend && npm run lint`：通过。
- `cd frontend && npm run build`：通过。
- `go test ./...`：通过。

## 遗留边界

- 镜头跟随缺少“用户正在拖动画布”守卫，按审查结论作为低风险后续项记录，不阻塞本次合入。
- 快照仍受浏览器 localStorage 总容量约束；本次只调整每图内部淘汰策略，没有迁移到服务端存储。
