## 完成内容

- 调整了关系连线默认路径：
  - 默认主连线不再强制绕到抬高的中点。
  - 改为优先连接两节点最近边缘，曲线只保留轻微导向，不再出现大幅拱起。
  - 对折线模式补了更直接的单/双折线路径与中点计算。
- 把关系线右键箭头设置改成了轮盘式交互：
  - 外圈四个选项分别为双向箭头、指向源节点、指向目标节点、无箭头。
  - 分叉和删除操作保留在轮盘下方。
- 修复了区域框交互：
  - `node-layer` 不再吞掉空白区域事件。
  - 区域框可被左键选中，选中后有高亮态。
  - 区域框可直接拖动，区域内节点继续联动移动。

## 验证结果

- `npm run build:web`
- `go test ./...`

以上均已通过。

## 备注

- 本轮没有处理版本号和打包。
- 工作区仍保留用户此前未归属到本轮的改动：
  - `frontend/src/preferences.ts`
  - 4 个 `docs/plan|summary-2026-04-13-*.md`
