## 完成内容

- 对齐了前后端文档结构：
  - `RelationEdge` 增加箭头方向、中点偏移、分叉分支、兼容旧版 waypoint 字段。
  - `Document` 增加 `regions`，保存和读取不再丢失区域框。
- 修复了区域框流程：
  - 右键菜单进入创建后，可通过左键拖拽建立区域框。
  - 区域框拖动时，框内节点会同向联动移动。
  - 区域框节点归属改为按节点包围盒与区域相交判定。
  - 3D 视图中的区域染色外圈也改为复用相同判定逻辑。
- 修复了关系线流程：
  - 修复 SVG 关系线左键无法命中的问题。
  - 关系线支持无箭头 / 单向 / 双向箭头。
  - 选中关系线后显示中点圆点并高亮关系线。
  - 中点圆点支持拖动移动。
  - 中点圆点支持长按后拖拽到目标节点建立分叉连接，形成真实连接而不是仅画自由线段。

## 验证结果

- `npm run build:web`
- `go test ./...`

以上均已通过。

## 遗留说明

- 本轮已补齐保存链路，但区域框创建、关系线选中和分叉建立仍建议在实际 UI 中再做一次手工回归，重点看：
  - 区域框创建后的手感与拖拽流畅度
  - 分叉线在复杂节点排布下的视觉路径是否还需要再优化
