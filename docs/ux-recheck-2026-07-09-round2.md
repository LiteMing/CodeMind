# CodeMind UX 修复复查清单（2026-07-09 第二轮）

针对分支 `fix/ux-checklist-2026-07-09` 的修复复测（批次1 `a09a142` + 批次2 `5e29955`，测试包 CodeMind-1.9.2.exe）。
只列**本轮修过的项**与其回归警戒；首轮已通过且本轮未触碰的项不必重测。
判定：勾选=通过；不通过在行尾记现象。

## 环境

- 桌面包 `build/bin/CodeMind-1.9.2.exe` 为主；浏览器 `npm run dev` 抽查 F 组帧率（F12 性能面板）。
- 数据：①空白新图；②30 节点日常图；③300+ 节点大图（沿用上轮数据）。

## A. 动画修复复查（UX-01/02/03，上轮 A2/A3/A4 失败项）

- [ ] R1 创建动画：Tab/Enter 建节点 → 有 scale+fade 出现动画，**且动画发生在节点原位**（不向右下漂移）
- [ ] R2 连续创建：快速连按 Tab 建 10 个（注意：现在按住不松只建 1 个，是防冻结特性）→ 每个节点都有动画、不闪现
- [ ] R3 删除动画：删单节点与删 10+ 子节点父节点 → 渐隐在原位；子树 stagger 错落
- [ ] R4 删除连线：删除带连线（层级线+关系线都要试）的节点 → **连线与节点同步淡出**，不再瞬间消失
- [ ] R5 折叠/展开：折叠 20+ 子分支 → 渐隐在原位、连线同步；展开反向
- [ ] R6 回归警戒：拖拽节点、编辑态（含自动宽度编辑）、选中高亮 → 节点定位与之前一致，无新漂移

## B. 小地图复查（UX-04，上轮 B6 失败项）

- [ ] R7 拖拽视口框：连续拖拽导航 → 镜头平滑跟随，**无异常快速跳变**
- [ ] R8 点击外围空白：点小地图四角/边缘空白区 → 镜头最多移到内容边缘附近（80px 边距内），**不再飞到极远**
- [ ] R9 回归警戒：点击小地图内容区居中、增删/折叠后小地图同步 → 与上轮一致正常

## C. 引导与工具条复查（UX-05/06，上轮 C1/C5 失败项）

- [ ] R10 空画布引导位置：新建空图 → 提示出现在根节点**下方**，不遮挡标题
- [ ] R11 引导文案：中英文均为"选中节点后按 Tab 创建子节点"语义
- [ ] R12 引导 dismiss：建第一个子节点 → 提示消失；**删光所有子节点 → 提示不再重现**（本次会话内）
- [ ] R13 工具条按钮：单选节点 → 四钮（颜色/优先级/删除/AI）**逐个点击均生效**（点图标正中也生效）
- [ ] R14 工具条位置：工具条在节点**正上方水平居中**（不再偏右）；节点贴屏幕边/顶时工具条不出屏（顶部无空间时落到节点下方）
- [ ] R15 回归警戒：多选/编辑中/开浮层时工具条隐藏；画布空白处拖拽平移仍正常

## D. 折叠动画时长复查（UX-07，上轮 F3 失败项）

- [ ] R16 大子树折叠：折叠 50+ 子节点分支 → 整个错落动画在 **~0.75s 内**完成（400ms 窗口 + 350ms 单节点动画），不再匀速拖到数秒
- [ ] R17 小子树折叠：折叠 5-8 子节点 → 仍保留 40ms 错落感（cap 不影响小规模）

## E. 剪切粘贴复查（UX-09，上轮 H2 失败项）

- [ ] R18 关系线保留：框选含关系线的多节点 Ctrl+X → Ctrl+V → 父子层级线**与关系线**都正确重建（含关系线标签/箭头方向）
- [ ] R19 部分选择：只选中关系线一端的节点复制粘贴 → 该关系线不复制、也不产生悬空线
- [ ] R20 回归警戒：Ctrl+C 复制粘贴、撤销粘贴 → 行为正常

## F. 大图性能复查（UX-08 短期项，上轮 F1/F5 失败项）

- [ ] R21 按住 Tab：大图上按住 Tab 不松 → **只创建 1 个节点**，画布不冻结
- [ ] R22 缩小视图帧率：300 节点 @40% 缩放平移 → 与上轮 15-20fps 对比记录提升幅度（阴影降级生效；预期有明显改善但未必到 60fps——虚拟化在后续 P4）
- [ ] R23 阴影切换：缩放跨过 70% 阈值 → 节点阴影深浅切换不突兀（记录观感）

## G. 布局复查（UX-10，上轮"新问题1"）

- [ ] R24 多行节点布局：给节点输入 3+ 行长文本确认 → 兄弟节点自动让位，**无重叠**
- [ ] R25 备注高度：给节点加长备注 → 同上
- [ ] R26 整理命令：对含多行大节点的分支执行"整理布局" → 垂直间距按实际高度分配，单行节点区域密度与之前接近
- [ ] R27 回归警戒：普通单行节点的自动布局结果与升级前观感一致（默认密度不变）；手动布局（关闭 autoLayoutOnCollapse）时编辑提交**不**触发重排

## H. 第三轮补修复查（1.9.4，人工批注遗留项清扫）

- [ ] R28 展开闪烁：折叠 20+ 子分支再展开 → 子节点从透明错落淡入，**无先亮后暗跳变**（1.9.3 修复项）
- [ ] R29 工具条颜色钮：选中节点点工具条颜色钮 → **节点颜色循环切换**（无色→slate→blue→teal→green→amber→rose→violet→无色）
- [ ] R30 当前节点面板折叠：Inspector"选中节点"标题可点击折叠/展开，与工作区/快照分区一致；默认展开
- [ ] R31 快捷键入口：工具栏快捷组出现 `?` 按钮、视图菜单出现"快捷键"项 → 点击打开覆盖层（C3）
- [ ] R32 覆盖层内容：覆盖层编辑分类含 Ctrl+X 剪切；docs/keyboard-shortcuts.md 含 Ctrl+X/Ctrl+//缩放键（C4）
- [ ] R33 toast 国际化：切英文界面后 删除/粘贴/AI更新/平台说明复制 的 toast 均为英文（D1）

## 未修项（预期仍失败，不必惊讶）

- A9 减弱动效开启方法：Windows 设置 → 辅助功能 → 视觉效果 → 关闭"动画效果"，重启应用后生效
- E 组 AI 可见性（前置快照/镜头跟随/ai-pending 标记）：排期在"AI 可回滚补丁"工单
- D2 保存三态指示、顶栏自定义：P4 待办（快捷键入口与 toast i18n 已在 H 组修复）
- F5 极限规模（500/1000 节点）：需视口虚拟化（P4 结构项）
- G 组触屏 pinch：待定位决策

## 结果记录

| 日期 | 环境 | 通过 | 失败 | 备注 |
|------|------|------|------|------|
|      |      |      |      |      |
